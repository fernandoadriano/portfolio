import React from 'react';
import styled from 'styled-components';

const ContactWrapper = styled.div``;

const Contact = () => (
  <ContactWrapper>Contato</ContactWrapper>
);

export default Contact;
