import React from 'react';
import styled from 'styled-components';

const ProjectsWrapper = styled.div``;

const Project = () => (
  <ProjectsWrapper>Portifólio de Projetos</ProjectsWrapper>
);

export default Project;
